#!/usr/bin/env python

from distutils.core import setup

setup(
    name         = 'powerlineshell',
    version      = '0.1',
    description  = 'Shell command segment for Powerline',
    packages     = ['powerlineshell']
)
